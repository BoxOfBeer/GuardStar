services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: guardstar
      POSTGRES_PASSWORD: guardstar
      POSTGRES_DB: guardstar
    ports:
      - "5432:5432"
    volumes:
      - guardstar_pgdata:/var/lib/postgresql/data

volumes:
  guardstar_pgdata:
