# Dev-log 2026-05-04

- Добавлен каталог **`agent/`** — бот на Python: сессия `requests`, эндпоинты по **`docs/api-player.md`**, решение шага через **OpenAI Chat Completions** (`response_format: json_object`). Точка входа: `python -m agent.main`, зависимости: `agent/requirements.txt`.
- В исполнителе постройки учтён формат **`placement_checks.results`**: словарь по типу здания (не список).
- **`agent/agent_log.py`**, расширенный промпт в **`agent/brain.py`**: журнал рассуждений (`reasoning_journal`, альтернативы, риски, «что исследовать дальше»); в **`agent/main.py`** — вывод контекста, размышлений, пошаговых API-действий и полного JSON модели в консоль и опционально в файл (`--log-file` / `GUARDSTAR_AGENT_LOG`).
- **`agent/main.py`**: докстринг без секретов; явное логирование `GUARDSTAR_BASE_URL`; при `ConnectionError`/`Timeout` — сообщение про удалённый хост (`https://…/` без `/api`) vs локальный `127.0.0.1:5000`; обработка сети на health/login/register/sync/execute.
- **`agent/game_client.py`**: для не-localhost `http://` → **`https://`**, чтобы после редиректа прокси POST `/api/login` не превращался в GET (типичный **HTTP 405**); понятный текст при неуспешном login.
- **`agent/brain.py`**: обработка **OpenAI PermissionDeniedError** (регион `unsupported_country_region_territory`) и др. ошибок SDK; опционально **`OPENAI_BASE_URL`**; в **`agent/main`** — аккуратный выход через `die` без полного трассека на `RuntimeError` из `decide_action`.

