# Dev-log 2026-05-02

- Продолжение линии discovery/HUD от 2026-05-01.
- UI `me.js`: кнопка руин/аномалий не сбрасывается при каждом `updateSelectedPanel`, если выбрана та же клетка (`discoveryHudCellKey`) — убрано мерцание «Исследовать» ↔ «Уже исследовано» при `loadWorldState` / `refreshSupplyHud`; интервал 3 с вызывает только `refreshWindow()` без второго `loadWorldState()`; версия `01.042`.
- HUD `me.html` / `me.js` / `styles.css`: строительство — под кнопкой строка «Нужны инженеры» (зачёркнута) и подсказка `?` с `title` про флот с инженерным кораблём; при отсутствии инженеров кнопка с классом зачёркивания. Исследование руин — при отсутствии флота на клетке кнопка «Исследовать» зачёркнута, строка с `?` («Нужен ваш флот на этой клетке.»); обёртка `hud-action-discovery-wrap`; версия `01.043`.
- HUD: убрана отдельная строка «Нужны инженеры»; `?` встроен в кнопки «Строительство» и «Исследовать» (`.hud-btn-with-help`). Завершённое исследование — подпись «Исследовано»; версия `01.044`.
- HUD/CSS: `?` в одну строку с текстом (`nowrap`, компактный `.hud-help-tip-inline`), выравнивание высоты блоков действий (`align-items: stretch`). Красный фон — только у кнопок с нехваткой условий (`.hud-btn-blocked`), карта исследованных руин снова бледно-зелёная; версия `01.045`.
- HUD: убрано зачёркивание подписей (`hud-action-struck`); для нехватки инженеров / флота остаётся красный фон `.hud-btn-blocked` и `?`; версия `01.046`.
- Карта `me.js` (`renderMap`): ландшафт без «домиков» — руины ⟁ / исследованные ◈, аномалия ◎, туманность ≋; форпост один символ ▣ (свой/чужой — цветом `outpost-ally` / `outpost-hostile`); прочие постройки на клетке ◇ вместо 💎; версия `01.047`.
- UI карты: режимы «Тактический» / «Графический» (SVG в `me.js`, fallback на глифы если `MAP_GRAPHIC_SVG_AVAILABLE === false`), чекбокс координат на клетках; `styles.css` — `.map-cell-icon`, тона, скрытие `.coord`; `me.html` — настройки в модалке, футер с текстом про cookie/сессию и логотип `static/img/guardstar-logo.png`; топбар без дубля «GuardStar»/имени в HUD; версия `01.048`.
- HUD `me.html` / `me.js`: Z-слой и размер окна + строка `#status` перенесены под заголовок «Карта сектора»; «Действия» между «Флот» и «Клетка»; блок `#hud-fleet-detail` (активный флот, координаты, сол, статус, ETA, дом) скрыт, если выбрана клетка без вашего флота; маркер клетки — без строки «нет (пусто)»; версия `01.049`.
- UI: панель под «Карта сектора» — Z-слой, окно и `#status` в одну строку (`styles.css`); убраны заголовок «HUD пилота» и блок «Пилот» (население/влияние — в модалке «Империя»); `me.js` без обновления удалённых `#hud-pop` / `#hud-influence-home`; версия `01.050`.
- Карта: размер клетки от ширины `.map-wrap` (`computeMapLayoutCellPx`), `ResizeObserver` + `applyMapCellSizeFromContainer`; сетка `page-grid` — `minmax(0,1fr)`, карточка сектора `min-width:0`; поле на всю ширину колонки; версия `01.051`.
- MMO-композиция и коммуникации: `me.html` — три колонки (комms + карта + HUD), вкладки Система / Общий / Альянс / Приват, перенос `#events` влево с чипами фильтра; `styles.css` — `.page-grid-mmo`, сворачиваемая панель, оверлей на узком экране; `me.js` — `localStorage`, маппинг `event.type` → категории, polling чата, ЛС и блокировки; бэкенд — миграция `20260502_000018` (`chat_messages`, `player_blocks`, `players.staff_chat_exempt`), `chat_service.py`, эндпоинты `/api/chat/*`, заглушки альянс-чата `501`; админка — переключатель staff-чата; тесты `tests/test_chat_api.py`; версия `01.052`.
- Чат: лимит тела **1000** символов без молчаливого усечения (`message_too_long` → **413**), опрос истории до **100** сообщений; в UI лента обрезается до **100** строк DOM, строки через **`textContent`**; клиентская проверка длины перед POST; версия `01.053`.
- Регистрация `register.html` / `web.py` / `styles.css`: свод «Правила пилота GuardStar» в прокрутке до имени и расы; чекбокс **Согласиться с правилами** (`accept_pilot_rules=1`) обязателен на сервере; кнопка «Создать пилота» неактивна, пока не отмечен чекбокс (скрипт + `required`); версия `01.054`.
- Миграция `20260502_000018`: `upgrade()` сделан **идемпотентным** (если `chat_messages` уже есть из `create_all`, добавляются только недостающие индексы / `player_blocks` / колонка `staff_chat_exempt`). На локальной БД выполнен `alembic upgrade head`.
- Карта: окно по умолчанию **13×13** / пресет **17×17**, кламп **13…25** на клиенте и в API (`api.py` / `world_service.py`); **− / +** и пресеты в модалке **«Настройки UI»**; на тулбаре карты только подпись текущего размера; `me.js` — `syncMapWindowUi`, без старых `radius-4`/`radius-10`. Трёхколоночный layout: **stretch** по высоте, левая/правая колонки с картой (`styles.css` + `me.html`). Термин **«Пилот»** в пользовательских текстах заменён на **«Оператор»** (`me.html`, `account`, `register`, `index`, `show_code`, `web.py`, `admin_hub`). Версия **01.055**.
- UI `styles.css`: на ширине **≥1200px** у `.page-grid-mmo` фиксированная высота ряда (`grid-template-rows: 1fr`, `calc(100dvh - 9.25rem)`), чат и HUD **внутри** этой высоты (`overflow` + скролл); **`.map-wrap`** — без горизонтальной прокрутки (`overflow-x: hidden`); полосы прокрутки — **6px**, тёмно-синие (`--scrollbar-*`, webkit + Firefox). Версия **01.056**.
- UI: откат фиксированной высоты MMO-сетки (центр снова по содержимому карты, без «коробки» с лишней прокруткой). Боковые панели: **`--mmo-sync-h`** = высота **`.map-sector-card`**, `align-items: start` + `ResizeObserver` / вызовы из `applyCommsLayout` и `applyMapCellSizeFromContainer` в **`me.js`**. Версия **01.057**.
- UI `/me`: формы общего и приватного чата **над** лентой; заголовок панели **«События»** / **«Чат»** по вкладке (`#comms-panel-title`, `syncCommsPanelTitle` в **`me.js`**). Десктоп: обёртка **`.me-play-layout`**, **липкий топбар** (`body:has(.me-play-layout)`), высота ряда под вьюпорт, **вертикальная прокрутка только у `.map-wrap`**; синхрон **`--mmo-sync-h`** отключена при `.me-play-layout`. Версия **01.058**.
- UI: свёрнутая коммуникация — колонка **`max-content`**, панель **`align-self: start`**, без фона/тени/растягивания на высоту ряда (`styles.css`); остаётся только кнопка разворота. Версия **01.059**.
- Роли **админ/модератор игры** в админке (`admin_hub.html`, POST в **`web.py`**) и на клиенте: модерация общего чата из игры (**скрыть** / **удалить** / **мут чата** / **бан аккаунта** для админа), пересбор ленты при смене прав; **`/me`**: блок настроек «убрать туман войны» + запрос **`reveal_fog=1`** к **`GET /api/world/window`** (`me.html`, **`me.js`**, **`styles.css`**); проверка **`account_disabled`** перед рендером **`me`**. Версия **01.060**.
- UI админа: пояснение про **ленивый** ландшафт (сиды) и окно БД; поля **X / Y / Z** + кнопка **«Перейти»** — сдвиг центра тактического окна для отладки генерации/флотов (`me.html`, **`me.js`**). Версия **01.061**.
- UI `/me` (≥1200px): **`html`/`body`** без вертикальной прокрутки страницы; **`app-shell`** — колонка flex на высоту вьюпорта; **`me-play-layout`** — `flex: 1` вместо `calc(100dvh - …)` под футер; футер с **`max-height` + внутренний скролл**; легенда/тулбар карты **`flex-shrink: 0`**, вкладки коммов — скролл внутри панели (`styles.css`). Версия **01.062**.
- UI `/me` (откат «клетки»): снова **естественный скролл страницы**; сетка **`--me-mmo-h`** ≈ высота окна — **центр под карту**, скролл у **`.map-wrap`**; **липкий топбар**; боковые **`position: sticky`** под топбаром; чаты — **`flex-direction: column-reverse`** + **`scrollTop = 0`** (`styles.css`, **`me.js`**). Версия **01.063**.

Дальнейшие записи — в **`dev-log-2026-05-03.md`** (с 3 мая 2026).
