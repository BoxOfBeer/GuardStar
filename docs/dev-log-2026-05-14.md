# Dev-log 2026-05-14

- UI `/me`: подложка `static/img/background.png` на `body` при наличии `.me-play-layout`, слои звёзд в `base.html` приглушены.
- Карта: класс `cell-empty-visible` в `me.js` (клетка видима и без «смыслового» содержимого по `mapCellHasMeaningfulContent`); заливка клетки прозрачная, сетка `.map-grid` и карточка сектора без сплошного фона; заголовок, тулбар и легенда с прежней «карточной» подложкой.
- Версия игры: `docs/GAME_VERSION` → `01.167`.

- **Колонизация:** `colonize_planet` — флот допускается на клетке планеты или на **соседней по гексу** (`hex_distance` ≤ 1), ошибка `fleet_not_adjacent_to_planet`; `_mixin_08_planets_world.py`.
- **Карта:** флот не перекрывается планетой — тактика: стек `.map-marker-stack` + порядок слоёв; графика: `.map-graphic-stack--planet-fleet` с смещением иконки флота; `me.js`, `styles.css`.
- **Доки/вики:** `docs/api-player.md`, подсказки в `me.html` про соседнюю клетку при колонизации.
- Версия: `docs/GAME_VERSION` → `01.168`.

- **Топбар `/me`:** чипы ресурсов (`button.res-chip`) снова с `width: auto` — глобальное `input, button { width: 100% }` растягивало каждую кнопку на всю ширину полосы, визуально оставался один ряд (например RP). У `#resources-chip.resources-bar` явно `display: flex; flex-direction: row`. В модалке «Империя» у `button.econ-wiki-res` / `--bare` — `width: auto`, чтобы строки расходов снова шли в ряд с подписями. `styles.css`. Версия → `01.171`.

- **Карта /me:** убран заголовок «Карта сектора»; тулбар Z/окно/status — первая «плашка» с прежней тенью, рамки панелей (`topbar`, коммы, HUD, карта, легенда) — `border-color: transparent`; планета ×2 (`.planet-icon`, класс `map-cell-icon-wrap--planet-scale` в `me.js`); сильнее свечение своих построек (`.building-ally`, `.map-cell-icon-wrap--field-building`). Версия → `01.172`.

- **`loadWorldState` (`me.js`):** сол (`#hud-tick`), топбар-ресурсы и RP обновляются **до** `fetchBalanceCached()`; запрос баланса в `try/catch`. Раньше любая ошибка/исключение при `/api/balance` до обновления HUD прерывала весь `try` — не обновлялись сол, события, правый HUD. Версия → `01.184`.
