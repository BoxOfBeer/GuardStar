# Dev-log — 2026-04-30

Формат: накопительно по дню; таймштамп в имени файла (`dev-log-YYYY-MM-DD.md`).

## Версия игры (семантика релиза)

- Введена отдельная строка релиза **`MM.mmm`** (major.minor) в `docs/GAME_VERSION`, отдаётся в `GET /api/version` как `game_version`.
- Ранее в проекте были только **`BUILD_ID`** (UTC-штамп при старте процесса, `server/app/build_info.py`) и **`balance_schema_version`** из `server/data/balance/meta.json` — формата `01.001` не было.

## Что наработано (архив чата / недавняя линия разработки)

### Влияние и контроль

- Источники влияния (планеты, форпосты), decay, накопительный контроль, множители от масштаба империи — логика в `world_service` и связанных моделях.

### Форпосты

- Сущности форпостов и модулей, баланс в `outposts.json` / `outpost_modules.json`.
- Стройка и апгрейд через API; обзор, влияние, боевой тик (обстрел в радиусе), upkeep → **offline** при нехватке ресурсов или вне снабжения.
- Минимальная дистанция между своими форпостами от **базового радиуса обзора** строящегося типа.

### Планеты

- Классы планет, слоты стройки, `supplier_count`, население/слоты; старт с вариацией для «людей».
- Лимиты **max_per_planet по типам зданий** убраны в пользу **общих слотов планеты** и подложки.

### Карта и UI

- Клетка с планетой отдаётся как `terrain: "planet"` (не пустой космос); клиент `me.js`, шаблон `me.html`, стили.
- Карточки зданий, проверки размещения, чекбокс «показать все варианты», инженер как расходник при полевой стройке.
- Жильё/лаборатория — только на клетке планеты (`planet_required` / `build_on_terrain: ["planet"]`).

### Снабжение — фаза A (без маршрутов)

- Радиус: **база 5 + 3 × supplier_count** на планете (константы на `WorldService`).
- **`supplier_count`** на планете, не юнит на карте; найм по `supplier_t1` из `units.json` (в `unit_aliases` алиас **`supplier` не добавляли**, чтобы тип не попал в логику флота).
- **`GET /api/supply/state`**, **`POST /api/supply/hire_supplier`**; события `supplier_hired`, `supply_radius_changed`.
- HUD: строка «Снабжение: есть / нет» и предупреждение; в модалке планеты — формула и кнопка найма.
- В `get_sector_stub` в `details` планеты: `supplier_count`, `supply_radius`, `supply_base`, `supply_per_supplier`.

### Флот и энергия

- У флота `energy` / `max_energy`; движение тратит энергию; upkeep с локальной энергии; реген только **в снабжении или на хабе** (планета / активный форпост).
- **Emergency return** при `energy == 0` вне снабжения; `order_type: emergency_return`; в UI — пунктир и подписи.

### Тесты

- В `test_balance.py` проверка наличия `supplier_t1` в балансе.

## От чего отказались / отложили

- **Фаза B снабжения**: маршруты, обрывы, стоимость доставки — **не в рамках фазы A**.
- Снабженец **не** как боевой юнит на карте — только счётчик на планете (MVP).
- Алиас `supplier` в `unit_aliases` — **намеренно не используем** для найма/флота.

## Что меняли по ходу

- Несколько раз правки не ложились из‑за расхождения контекста — лечилось перечитыванием файлов.
- Уточнение модели: снабжение привязано к планете и `supplier_count`, не к отдельному юниту на секторе.

## Сегодня (эта сессия): документация и процесс

- Папка `docs/`: ежедневный **dev-log** с датой в имени; файл **`GAME_VERSION`** для релизной строки `MM.mmm`.
- Правила Cursor: поднимать `GAME_VERSION` при существенных правках; периодически сверять и дополнять `docs/dev-log-*.md`.
- API и UI: в ответ `/api/version` добавлено поле `game_version`; чип версии в клиенте показывает релизную строку, если она пришла.

## Позже в этот день: аварийный возврат флота

- **`_nearest_return_hub`**: среди хабов на одинаковой дистанции предпочитает клетку **без флота**, чтобы реже упираться в занятую клетку.
- **`emergency_return` при враге на хабе**: не `pending_combat`, а постановка на **свободную соседнюю клетку** (`emergency_orbit_staging`); при отсутствии клетки — `emergency_no_staging_cell`.
- **Свой флот на хабе** при аварийном возврате — отдельное событие без возврата топлива (топливо не списывалось).
- **`_fleet_adjacent_to_enemy_occupied_hub`**: не создавать новый `emergency_return` каждый тик, если флот уже «кольцует» врага у хаба (антиспам / антиосцилляция).
- Версия **`01.002`**.

## Идеи в бэклоге (из чата)

- Снабжение v2: маршрут от ближайшего хаба, стоимость доставки, обрыв при угрозе.
- Флот: расширить одну модель энергии до набора потребностей (топливо, провизия и т.д.).
- Emergency return: дальше — явная смена хаба, запрет отмены игроком (опционально), лимиты событий в логе.
- Проход по балансу и сценарии «застрял флот».
