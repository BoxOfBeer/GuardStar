# Dev-log 2026-05-01

- Админка `/admin/world`: версия игры/`BUILD_ID`/схема баланса; блок автосола с состоянием воркера; настройка `player_spawn_min_manhattan` (мин. дистанция Манхэттена для новой домашней планеты при регистрации), форма `POST /admin/world/spawn`.
- Игровой топбар: убраны чип версии API и переключатель автосола (управление только из админки).
- Персистентность: колонка `world_state.player_spawn_min_manhattan` (миграция `20260502_000014`), safety-net в `app/__init__.py`.
- `WorldService._pick_start_pos` читает минимум из `world_state`.
- При старте приложения: если задан `ADMIN_TOKEN` и `admin_config.admin_token_hash` пуст — записывается хэш (для локального доступа к `/admin/*` без ручного SQL).
- Проверено: неинтерактивный SSH к прод-хосту GuardDoc с ключом `id_ed25519_guarddoc` (`ssh -i ... -o BatchMode=yes`).
- Добавлен `docs/deploy-guarddoc-subdomain.md`: один инстанс + nginx, смена домена без дублирования приложения, чеклист переезда dev→боевой хост.
- Отдельная БД для GuardStar от school-домена описана в deploy-доке; плейтест без GitHub: `players.feedback_audited`, таблица `feedback_playtest_api_logs`, хуки на `/api` (только мутации), переключатель в `/admin/accounts`, миграция `20260502_000015`.
- План плейтеста: апгрейды зданий `POST /api/buildings/upgrade` + цепочки `upgrade` в `buildings.json`; `basic_water_t0`/`starter_*`; гражданские NPC-транзиты (`order_type=npc_transit`, спавн вне видимости, очистка при прибытии) в `world_service.py`; расы combat-множителей в `races.json`, у корсаров `race_id=zenith`.
- Исследования: `players.research_points` (миграция `20260501_000016`), начисление RP с тика + руины (`discovery_service`), стоимость `research_points_cost` / `residual_time_ticks` в `tech.json`; лабы — upkeep/strain в `economy.json`; `GET /api/economy/summary` и `GET /api/tech/state` отдают RP; контент C+: `tech_deep_scan_1`, `corvette_t1` + алиас `corvette`.
- Документация: обновлён `server/README.md` (dev-MVP “что умеет сейчас”: солы/снабжение/форпосты/discovery/исследования/RP/админка), миграции в примерах через `python -m alembic`.
- CI: добавлен GitHub Actions workflow `.github/workflows/pytest.yml` (pytest для `server/tests/test_balance.py` на push/PR, без PostgreSQL).
- Архитектура: добавлен `docs/ARCHITECTURE.md` (карта слоёв/сервисов и поток тика).
- Рефактор: вынесена сводка экономики для UI в `server/app/services/economy_service.py`, `WorldService.get_economy_summary()` теперь делегирует туда.
- Аудит: в `WorldService._emit_event()` добавлен `payload.source` по умолчанию (`server_tick`) для унификации источников действий в событиях.
- Форматирование: прогнан `ruff format` по серверному Python-коду, добавлены `.gitattributes` и `.editorconfig` (защита от «файлов одной строкой» и мусора в diff).
- Баланс: `races.json` — выровнены ключи `production_multiplier` (food/water явно = 1.0 у human/zenith), `buildings.json` — добавлены описания для `research_lab_t1` и `drydock_mini_t1`.
- Регистрация: добавлен выбор расы при создании пилота (`/register` и `POST /api/register` принимают `race_id` и валидируют по balance).
- UI: кнопки строительства получили информативные tooltip'ы (имя/описание/эффект/террейн) через `meta` из `/api/buildings/placement_checks`.
- Рефактор (шаг 2): экономический блок тика (производство планет + логистика снабжения + имперский upkeep флотов) вынесен в `EconomyService.apply_economy_tick()` и вызывается из `WorldService.process_next_tick()`.
- Web UI: модалки состава/создания флота подтягивают типы кораблей из `aliases.unit_aliases` через `/api/balance` (плюс кэш), вместо трёх захардкоженных строк; версия игры поднята до `01.018`.
- Баланс: в `aliases.json` добавлены алиасы флота `supplier → supplier_t1`, `freighter → freighter_t1` (юниты уже были в `units.json`).
- Плейтест: добавлен `docs/PLAYTEST.md` (чеклист хостинга), в `server/README.md` — краткая секция; переменная `GUARDSTAR_DB_SAFETY_NET` (в `config.py`): при `false` отключаются `create_all()` и аварийные SQL-патчи в `create_app()` — схема только через Alembic; в `server/.env.example` — комментарий; `docs/ARCHITECTURE.md` — примечание про safety-net; версия `01.019`.
- Админка и фидбек: единая страница `GET /admin` (`admin_hub.html`) — блоки «Мир», «Аккаунты», «Обратная связь», ввод/смена токена в UI и в `?token=`; редиректы `/admin/world` и `/admin/accounts` → `/admin` с `tab=`; игроки — `GET/POST /feedback` (`feedback.html`), ссылка из топбара `/me`, подсказка про Discord и канал «баги»; таблица `feedback_messages` (миграция `20260508_000017`), модель `FeedbackMessage`; дефолт `ADMIN_TOKEN=1662916644` если env не задан; версия `01.020`.
- Локальный dev: в `server/run.py` по умолчанию включён Flask `use_reloader` (новые маршруты подхватываются без ручного перезапуска); отключение — `GUARDSTAR_NO_RELOADER=1`.
- Админ-токен: при старте `create_app` хэш в `admin_config` всегда перезаписывается из непустого `ADMIN_TOKEN` (устраняет рассинхрон после смены дефолта/`.env`); при ошибке — `warning` в лог вместо silent `pass`; версия `01.021`.
- Админка: в шапке показано подключение к PostgreSQL (хост/имя БД без пароля) и пояснение, что «0 аккаунтов» = другая БД или пустая таблица, а не сброс при F5; версия `01.022`.
- Исправление потери данных: `tests/conftest.py` больше не использует `DATABASE_URL` для `drop_all` — только `TEST_DATABASE_URL`; без неё DB-тесты skip (раньше `pytest` мог стереть всех игроков в `guardstar`); обновлены `server/README.md`, `.env.example`; версия `01.023`.
- Тесты: интеграция без `drop_all`/`TRUNCATE` — `tests/integration_helpers.py`: пилоты `gs_py_*`, удаление каскадом как у аккаунта, автоподчистка префикса после каждого теста, сброс `world_state.current_tick` между тестами, `create_all` один раз на сессию; версия `01.025`.
- Регистрация: выбор расы карточками вместо `<select>`; в `races.json` — `glyph`, `perks[]`, `lore` (художественный текст в `<details>`); стили в `styles.css`; при ошибке формы сохраняется `selected_race_id`; версия `01.026`.
- UI: код доступа на `show_code.html` и при показе нового ключа в `account.html` — крупный, подчёркнутый, красноватый акцент и рамка; версия `01.027`.
- HUD: убраны ручной «Сол», ссылки «Обратная связь» и «Выйти» с `/me` (остался «Аккаунт»); «Обратная связь» и выход — в `/account`; ручной сол — `POST /admin/world/tick_once` в админке; версия `01.028`.
- UI иерархия (`/me`): CSS-токены `--action` / `--surface-elevated`, кнопки `.btn-primary` / `.btn-secondary` / `.btn-danger`; топбар — ресурсы чипами `.res-chip`, «Империя» как primary навигации; карта — градиент фона, `.cell-selected` и glow зелёным, `.cell-visible` / контраст тумана, `.cell-active-fleet`, крупнее иконки и hover-scale; HUD — секции `.hud-section`, KPI для координат/статуса; модалка экономики — карточки `.econ-card` и сетка ресурсов; исследования — `.tech-item-active`, «Старт» primary, вкладки на зелёном акценте; регистрация — усилен выбор расы, CTA «Создать пилота»; `me.js` — разметка экономики, классы клеток; `races.json` — короткие перки; версия `01.029`.
- Экономика/UX: `economy.json` — базовая еда/вода планеты 9/9; старт населения 600; сводка `construction_reference` (справочные суммы цен построек и кораблей); топбар и модалки — наука (RP); `GET /api/tech/state` — `research_points_per_sol`; вкладка «Доступные» без тех с незакрытыми предпосылками, дефолт вкладки «доступные»; карточки исследований — длительность по `residual_time_ticks`, оплата науки+ресурсы; планета — русские названия построек из баланса, производство «за сол», юниты с подписями; эффекты построек в подсказках — «/сол» вместо «/тик»; `GET /api/world/state` — RP и `production_per_sol`; версия `01.030`.
- Баланс: `base_planet_production.crystal` 3→5 и `game_rules.calc_planet_production` (кристалл за уровень) — запас кристалла при трёх флотах и имперском upkeep 3/сол; версия `01.031`.
- UI: топбар ресурсов — без переноса строки (`nowrap`), до 900px и горизонтальный скролл при нехватке места; RP без скобок вокруг прироста, подсказка `title`; версия `01.032`.
- Багфикс: «третий флот» на карте при пустом составе — после расхода инженера оставалась строка `fleets` с `qty=0`; `get_world_state` её скрывал, а карта/`get_sector_stub`/`get_player_map_window` — нет. `_write_fleet_units` при 0 кораблях удаляет флот и шлёт `fleet_disbanded`; в данных клетки/окна карты пустые флоты не отдаются.
- Флот UI (`me.js`/`me.html`/`styles.css`): модалка состава — карточки кораблей по ролям (разведка / бой / техника и логистика), глиф, цена, статы, описание из баланса; блок «Склад… (оценка до сохранения)» с Δ ресурсов как на сервере; слияние / разделение / расформирование — отдельный оверлей `#fleet-sub-overlay`; при закрытии основной модалки закрывается и вложенный.
- Полевая стройка: несколько флотов с инженерами на одной клетке — выбор `#sector-builder-fleet`, `placement_checks` и `place`/`outposts/build` получают выбранный `fleet_id`.
- Сервер: `_fleet_units_map` — если сумма `fleet_ships` меньше legacy `fleets.qty`, недостающее добивается в `fleets.unit_type` (восстановление «потерянного» скаута при рассинхроне строк и qty); `_owned_engineer_fleet_at` — `order_by(created_at)` для стабильного выбора без `fleet_id`.
- Баланс: в `units.json` — краткие `description` у разведчика и истребителя; версия `01.033`.
- QoL карта: перетаскивание флота на клетку с другим своим флотом — модалка «Объединить / Рядом / Отмена» (`openFleetStackingChoiceModal`); «Рядом» ищет соседнюю клетку без второго своего флота (окно + `/api/world/sector`) и открывает обычное подтверждение полёта; то же при ошибке `cell_occupied_by_own_fleet` с кнопки «Лететь»; версия `01.034`.
- Снабжение и приказ «лететь»: в `WorldService.create_fleet_move_order` при клетке в зоне снабжения (`_is_cell_supplied`) перед проверкой стоимости движения энергия флота доводится до `max_energy` (иначе E=0 на астероиде в сети блокировал перелёт при большом upkeep).
- HUD «Строительство»: активно только при своей планете на клетке или флоте с инженерами; подпись «Нужны инженеры» (`#build-btn-hint`); после успешной полевой постройки модалка закрывается вместо повторного `fillPlanetModalFromApi` с текстом про инженера; версия `01.035`.
- UI: исправлен размер кнопки «Строительство» — `flex: 1` больше не применяется к кнопке внутри `.hud-action-build-wrap` (селектор `.hud-actions > button`); модалка сектора — расходы форпоста из баланса + «−1 инженер»; `GET /api/balance` отдаёт `outposts[]`.
- Мир: спавн гражданского `npc_transit` больше не требует, чтобы оба конца маршрута были вне обзора игроков — конвои могут появляться на карте и исчезать по прибытии; версия `01.036`.
- Бой: `_apply_outpost_combat_tick` больше не игнорирует всех NPC — обстреливаются враждебные ИИ-флоты (корсары); гражданский транзит по-прежнему не целится; событие `outpost_fire` по ИИ уходит владельцу форпоста; версия `01.037`.
- Карта `/me`: клик по **краю** окна — если в клетке есть объекты/непустой ландшафт, первый клик **только выбор** клетки, второй клик по той же клетке сдвигает окно; пустая клетка на краю по-прежнему сразу панорамирует.
- Флот: `max_energy` и реген в снабжении масштабируются с `_fleet_upkeep_energy_total` (как стоимость клетки маршрута): `max = min(8000, max(100, upkeep×12))`, реген `max(2, (max+24)//25)`; `_sync_fleet_energy_scale` после смены состава и в тике энергии / перед приказом move / при регистрации; версия `01.038`.
- Флот: `apply_fleet_upkeep_tick` больше не списывает локальную энергию за сол, если флот в **снабжении**, на **клетке своей планеты** или **своего активного форпоста** (`_fleet_skip_local_energy_upkeep`) — иначе upkeep по составу съедал больше регена и E залипал на 0; версия `01.039`.
- Бой: обстрел форпоста вызывает `_apply_fleet_post_combat_losses(..., allow_eliminate_fleet=True)` — снят жёсткий запрет потерь при `tot<=1`, из-за которого последний корабль врага под форпостом не уничтожался; версия `01.040`.
- Бой/UI: при полном уничтожении цели форпостом учитывается исчезновение флота — событие `outpost_fire` с `fleet_destroyed` и текст «уничтожил вражеский флот» / «уничтожил ваш флот»; discovery — только со своим флотом на клетке (`fleet_required`), `try_resolve_ruins_anomaly_for_sector` возвращает `headline` в ответе API; панель `/me` — поколение fetch сектора против мерцания кнопки «Исследовать»; окно карты — в туманности на своей территории (радиус колоний) без своей постройки на клетке чужой флот скрыт с вашей карты (у противника свой обзор сохраняется); `flags.cell_tint` / `ruins_surveyed`, глиф руин 🏠 после обзора; тон клетки и подсветка своих/чужих построек; версия `01.041`.
