# Dev-log 2026-05-03

Продолжение линии UI/MMO и админки от **2026-05-02**; новые фиксы и версии — **с этой даты** в этом файле.

- **`GET /api/world/state` быстрее:** повторный сбор **`_collect_influence_sources`** убран — тот же список передаётся в **`EconomyService.get_economy_summary(..., for_hud_poll=True)`** (без запроса всех ресурсов по планетам для казны, без подсчёта «стоимости восстановления» построек/флота и без лишних строк). События: **14** последних, **`_slim_event_payload_for_world_state_poll`** (без тяжёлых полей вроде `composition`, кроме `fleet_combat` / `combat_prompt_arrival`). **`GAME_VERSION` → `01.119`**.

- **Засада из руин / «зависание» тика:** `_purge_dangling_bandit_fleets` больше **не удаляет** именованную **«Засада»** без охоты (стационарная засада из discovery); по-прежнему чистятся пустые флоты, **`strike_origin_outpost_id`** и **«Ударное звено»** без цели. Поиск цели в **`_spawn_mvp_bandit_patrol_near`** — **bbox** вместо полного скана **`fleets`**. В **`_apply_influence_control_tick`** — верхняя граница радиуса источника (**96**), страховка от аномально большого **`r`**. **`GAME_VERSION` → `01.118`**.

- **Economy overrides в БД:** колонка **`world_state.admin_economy_overrides_json`**, миграция **`20260516_000027`**, **`world_economy_runtime.deep_merge_eco`**, **`_merged_pack_economy`** (**`_mixin_02`**), боевой/НПС-контур читает слитый **`economy`** (**`_warfare_economy`**, **`_mixin_05`** транзит/лабы, **`dev_schema_safety_net`**). Админка: вкладка **Игрок / баланс** — textarea + **`POST /admin/world/economy-overrides`**; в **`admin_dev`** — флаг **`admin_economy_overrides_active`**. Расширен **`economy.json`** (спавн дикой зоны, патруль/удар, сетка NPC-транзита). Тест **`test_world_economy_runtime.py`**. **`GAME_VERSION` → `01.117`**.

- **Админка — «консоль мира»:** вкладки **НПС / корсары** (ядерный стоп + раздельные блоки: флоты игроков, транзит, шахты, форпосты, прочие флоты корсаров; purge) и **Игрок / баланс** (`admin_max_fleet_units`, множители по **tier** для длительности и **research_points_cost** в `POST /api/tech/start`). Колонки **`world_state`** + миграция **`20260515_000026`**, **`world_research_runtime.py`**, правки **`_mixin_02/04/05/07`**, **`routes_progress`**, **`dev_schema_safety_net`**. **`GAME_VERSION` → `01.116`**.

- **Корсары снова каждый сол:** после ускорения окна карты вернули **`bandit_wilderness.ai_every_n_ticks`** с **5** на **1** в **`economy.json`** (патруль / дикая зона / рейдеры / purge — каждый игровой тик, как до разреживания под нагрузку тика). **`GAME_VERSION` → `01.115`**.

- **`GET /api/world/window` тормозил:** на каждую **видимую** клетку вызывалось **`_is_cell_supplied`** → повторные **`SELECT` планет**, радиусы с техами на **каждую** планету и **`SELECT` флота на каждую клетку L-пути** (при **`reveal_fog`** и радиусе **9** — сотни/тысячи запросов на один ответ). Добавлены **`map_window_supply_precalc`**, **`is_cell_supplied_from_precalc`**, **`planet_supply_rows_for_owner`** и один bbox-запрос чужих флотов (**`supply_service.py`**); окно карты использует precalc (**`_mixin_03.get_player_map_window`**). Опциональные **`player_supply_mods`** в **`planet_supply_radius`** — меньше дублей в **`planet_supply_candidates`**. **`GAME_VERSION` → `01.114`**.

- **Тик «туго» / тест корсаров:** блок корсаров (**патруль, дикая зона, рейдеры, purge**) выполняется только каждые **`bandit_wilderness.ai_every_n_ticks`** игровых солей (по умолчанию **5** в **`economy.json`**). Новый ряд **`world_state.test_block_new_fleets`** + API **`POST /api/world/admin/dev/fleet_spawn_lock`** и **`POST /api/world/admin/dev/purge_bandits`** (только **`is_game_admin`**); в **`GET /api/world/state`** у админа — **`admin_dev`**. Интервал автотика по умолчанию для **новых** строк **`world_state` / safety-net** — **10 с** (уже созданная БД не меняется). Миграция **`20260514_000025`**. **`GAME_VERSION` → `01.113`**.

- **`get_world_state` — меньше запросов к БД:** один **`SELECT` флотов игрока** (упорядоченный) на весь ответ: материализация legacy, выбор «главного» флота, **`fleets`**, блок **`upkeep_energy`**; второй вызов **`_collect_influence_sources`** убран — те же источники для дельты планеты и HUD влияния. **`GAME_VERSION` → `01.112`** (`_mixin_06`).

- **Тик мира «тупит» после корсаров:** патруль каждый сол делал **`SELECT * FROM fleets`** на **каждый** патрульный флот (`_apply_bandit_patrol_move_tick`); ударные звена — тот же полный скан на **каждый** форпост в проходе. Заменено на **один** список кандидатов (игроки, не NPC) для патруля и **один** запрос для целей strike на проход; событие **`bandit_patrol_spawned`** — узкий bbox по клеткам. **`GAME_VERSION` → `01.111`** (`_mixin_07`).

- **`/me` пустая карта / HUD «—» без ошибок в консоли:** анти-гонка через **`mapWindowFetchGen`** могла отбросить **единственный успешный** ответ окна, если параллельный **`refreshWindow`** завершился раньше с **401** (другой хост сессии) или иначе не обновил данные. Заменено на **очередь** `mapWindowRefreshQueue` (строго последовательные запросы окна + `loadWorldState`). **`GAME_VERSION` → `01.110`** (`me.js`).

- **Корсары — слишком много флотов НПС:** лимит был только **`max_outposts_per_chunk`** (на каждый чанк карты отдельно), плюс ударные звенья при потере цели обнуляли **`hunt_target_fleet_id`**, но строка флота оставалась — «зомби» копились. Добавлены **`bandit_wilderness.max_outposts_world`** (в **`economy.json`** по умолчанию **8**) и **`_purge_dangling_bandit_fleets`** после тика рейдеров (нет патруля у форпоста, нет охоты — снятие пустых/«Засада»/«Ударное звено»/с **`strike_origin_outpost_id`**). **`GAME_VERSION` → `01.109`** (`_mixin_07`, `_mixin_06`, **`economy.json`**).

- **Карта `/me` — «таскает» между зонами:** параллельные **`GET /api/world/window`** (панорама + `setInterval`) могли применять ответы не по порядку и перезаписывать **`viewCenter`** устаревшим центром — счётчик **`mapWindowFetchGen`**, нормализация **`center_x/y`** (`Math.round` / `Number.isFinite`). **Корсары кружат без боя:** **`_cell_blocked_for_fleet`** блокировала любую клетку с флотом; патруль и ударное звено не заходили на клетку жертвы — флаг **`enter_to_attack_fleet_id`** в **`_mixin_07`** (охота). **`GAME_VERSION` → `01.108`** (`me.js`, `_mixin_05`, `_mixin_07`).

- **Корсары: патруль у форпоста vs ударное звено:** у каждого корсарского форпоста — орбитальный **патруль** (**4–5** `fighter`, орбита ≤ `orbit_radius_manhattan_max`, агро **`aggro_radius_manhattan`**, респавн **15–25 сол**), события **`bandit_patrol_spawned`** игрокам рядом с базой. **Ударное звено** (**1 scout + 3–5 fighter + 3–5 corvette**), КД **150–200 сол**, глобально не более **`max_spawn_per_tick_global`** за один проход **`bandit_wilderness`**; поля **`strike_origin_outpost_id`**, **`bandit_hunt_announced`**, сообщения **`bandit_strike_spawned`** / **`bandit_strike_chasing`** с координатами форпоста. Миграция **`20260513_000024`**, правки **`economy.json`** (`bandit_patrol_wing`), **`fleet`/`outpost`**, **`me.js`**. **`GAME_VERSION` → `01.107`**.

- **Корсары — бой при контакте:** охотничьи звенья имели только сближение; при входе на клетку флота игрока бой не запускался. Добавлен вызов **`_resolve_fleet_vs_fleet_combat`** в **`_apply_bandit_raider_move_tick`**; стартовая засада (**`_spawn_mvp_bandit_patrol_near`**) получает **`hunt_target_fleet_id`** на ближайший живой человеческий флот. **`run.py`**: на Windows реloader по умолчанию выключен (вкл. явно через **`GUARDSTAR_USE_RELOADER=1`**), чтобы реже ловить **WinError 10038** при остановке. **`GAME_VERSION` → `01.106`**.

- **Внешний захват построек и обстрел форпостов:** прогресс **0.25/сол** до порога **`field_building_capture.resistance_default`** при чужом флоте на клетке (события `field_building_capture_tick`, `building_captured` / `building_lost_capture`); флот игрока в манхэттен‑радиусе **`outpost_bombard.fleet_range_manhattan`** наносит урон **`outposts.hp_current`** (`fleet_bombards_outpost`, `outpost_under_bombardment`, `outpost_destroyed`). Корсары: блок **`bandit_wilderness`** (~чанк **`chunk_side`**), лимиты шахт/форпостов, ударные звенья (**`bandit_strike_wing`**), движение охотников **`hunt_target_fleet_id`**. Миксин **`_mixin_07`**, тик **`_mixin_06`**, миграция **`20260512_000023`**, **`dev_schema_safety_net`**, **`economy.json`**, боевые типы и бейдж **`!`** в.system логе (**`me.js`**, **`styles.css`**). API форпоста: **`combat.hp_max`** / **`hp_current`**. При апгрейде корпуса HP масштабируется (**`_mixin_03.upgrade_outpost`**). **`GAME_VERSION` → `01.105`**.

- **Модалка флота (± / раскрытие):** у кнопок шагов не было классов **`btn-*`**, действовало глобальное **`button { width:100% }`** — плюс/минус растягивались, поле числа схлопывалось. Заданы фиксированные размеры **`fleet-qty-step`** / **`fleet-qty-input`**, контейнер **`inline-flex` + `width: max-content`**. Сетка строки слота — **`align-items: start`**, чтобы при открытом **`<details>`** блок числа не выравнивался по центру высоты и не наезжал на текст. **`GAME_VERSION` → `01.104`** (`styles.css`).
- **Лимит слотов колонии:** `build_slots_total` (рандом при создании игрока, см. **`ensure_player_has_start`**) ограничивает только постройки **на тайле колонии** (`_surface_slot_buildings_count_for_planet`). Полевая экспансия по карте этого счётчика **не расходует**. UI колонии: «**слоты поверхности**» + при наличии **полевых** — счётчик; в **`details`** планеты в API сектора — **`field_buildings`**, **`planet_buildings_anywhere`**. **`GAME_VERSION` → `01.103`** (`_mixin_01`, `_mixin_03`, `_mixin_04`, **`me.js`**).
- **Империя — счётчики построек (UI):** подписи **тайл / полевые** и блок **итого**; текст согласован с правилом **01.103** (лимит только тайл). **`GAME_VERSION` → `01.102`** — правки **`me.js`** до смены правила слотов.
- **Модалка «Состав флота»:** сетка кораблей с **`minmax(200px, …)`** при узкой средней колонке давала **две** колонки ~195px — в строке не оставалось места под русское название (остались глиф и ±). Исправление: **`minmax(min(280px, 100%), 1fr)`**, строка слота — **`grid` 1fr + auto**, компактное поле **`fleet-qty-input`**, плотнее фон оверлея и карточки **`#fleet-modal-overlay`**, чтобы не просвечивали карта и статусы вроде «нет слотов». **`GAME_VERSION` → `01.101`** (`styles.css`).
- **Модалка «Империя — экономика»:** раскрытые блоки **«Как читать таблицу»** и **«Строительство… / флоты»** запоминаются в **`localStorage`** (`econModalDetailsReadTable` / `econModalDetailsBuildings` в `gs.economyModal.v1`) и не схлопываются при авто‑обновлении. Счётчики построек **на планете / вне планеты** считают клетку по **`_cell_has_planet`** (как при стройке), а не по **procgen**‑террейну (`get_cell_terrain` никогда не был `"planet"`). В расходах добавлена строка **«Всего списано за сол»** и короткое пояснение, откуда берутся металл/кристалл/энергия/топливо в сверке с **чистым притоком** (`expenses_aggregate_per_sol` = производство − net). **`GAME_VERSION` → `01.100`** (`economy_service.py`, **`me.js`**).
- **Форпост / модули (тексты UI):** вместо слова «тик» игрок видит **солы** (`solWord`) и понятный статус «дождитесь готовности»; дельта содержания модулей в сводке — **/сол**. **`GAME_VERSION` → `01.099`** (`me.js`).
- **Форпосты и модули (баланс + поведение):** логистика еды/воды по линии снабжения масштабируется уровнем корпуса **×2/×4/×8** (база из `economy.supply_route_upkeep` как раньше). Содержание корпуса по тиру **металл/кристалл/топливо** скорректировано (**1‑1‑2 / 2‑2‑3 / 3‑3‑4**, энергия **2/3/4**). Улучшение корпуса по-прежнему **без инженеров** (ресурсы с домашней планеты), уточнено в модалке. Модули: **ставка только инженерами** (**1-й слот → 1, 2-й → 2, 3-й → 3**), без металла/кристалла из баланса; старт монтажа/апгрейда с **`status=in_progress`**, завершение в тике (`_resolve_completed_outpost_modules`); апгрейд через **`pending_module_type`**. **Один** активный монтаж на империю → ответ **`module_work_queue_full`**, флаг **`empire_module_work_busy`** в секторе. Поле **`effects_summary_ru`**, апгрейд — **`upgrade.summary_ru`**. Миграция **`20260511_000022_outpost_module_pending_type`**. **`GAME_VERSION` → `01.098`** (`outposts.json`, `outpost_modules.json`, `outpost_service.py`, `economy_service.py`, `_mixin_01`, `_mixin_03`, `_mixin_06`, `OutpostModule`, **`me.js`**).

- Разборка монолитного `server/app/__init__.py`: safety-net SQL → `app/db/dev_schema_safety_net.py`, импорт моделей → `app/db/models_registry.py` (в т.ч. `ChatMessage`, `PlayerBlock` для `metadata`), пост-старт → `app/startup.py`. Поведение `create_app()` без изменений смысла.
- Монолит `world_service.py` → пакет `app/services/world_service/`: `_deps.py`, `constants.py`, шесть миксинов `_mixin_01`…`_mixin_06`, публичный `WorldService` в `__init__.py`; импорты `from app.services.world_service import WorldService` без смены. Тесты `test_balance` читают исходники пакета целиком.
- Монолиты `routes/api.py` и `routes/web.py` → пакеты `app/routes/api/` и `app/routes/web/` (blueprint + `common` + сегменты маршрутов); `from app.routes.api import api_bp` / `web_bp` без смены.
- По API: `routes_progress_chat.py` → `routes_progress.py` + `routes_chat.py`; убран `deps.py` и `from deps import *` во всех сегментах — **явные импорты** в каждом `routes_*.py` (раньше при загрузке тянулся весь слой; теперь видно зависимости и меньше риск теневых имён).
- Web: `public_routes` / `admin_routes` — явные импорты вместо `deps.py` (файл удалён). `world_service`: во всех `_mixin_*.py` убран `from constants import *`, константы импортируются явным списком; `_deps import *` пока оставлен (ORM/сервисы — отдельный большой шаг).
- **HUD / «флот не найден»:** в `get_world_state` список `fleets` и активный флот отбирались по устаревшему `Fleet.qty`; клетка сектора уже собирается из `fleet_ships`. При расхождении (`qty=0`, строки в `fleet_ships` есть) список флотов становился пустым. Фильтр и отображаемое `qty` переведены на сумму состава через `_fleet_units_map` (+ хелпер `_fleet_total_units`). Bump **`GAME_VERSION`** → `01.064`.
- **500 на `/api/world/state`:** после разбиения `WorldService` в `_mixin_02._planet_production_deltas` остался вызов `WorldService._planet_influence_production_multiplier` — имя класса в миксине не определено; заменено на `self._planet_influence_production_multiplier`. Bump **`GAME_VERSION`** → `01.065`.
- **Центральная карта /me и путь полёта:** сетка могла быть выше обёртки (cell px только по ширине) → вертикальный скролл у `.map-wrap` и расхождение SVG пути с сеткой. `computeMapLayoutCellPx`: размер клетки от `min(ширина, высота)`; оверлеи SVG в том же координатном пространстве, что и `#map-zone-overlay`. `.map-wrap` — `overflow: hidden`, центрирование слоя без прокрутки; убран лишний `scroll`‑листенер для пути. Bump **`GAME_VERSION`** → `01.066`. Файлы: `server/app/static/me.js`, `server/app/static/styles.css`.
- **Карта на всю площадь слота:** единый квадратный `min(byW, byH)` оставлял пустые поля по одной оси («ужатая» карта). Отдельные **`--map-layout-cell-w`** / **`--map-layout-cell-h`**, `computeMapLayoutCellsPx`; сетка `size×size` заполняет ширину и высоту `.map-wrap` (прямоугольные клетки при разном соотношении сторон). Зоны и путь полёта — те же оси px; `.map-layer` без `max-width`/`max-height`, убрана отладочная рамка оверлея. Bump **`GAME_VERSION`** → `01.067`.
- **`/me` туториал входа:** модальное окно с блоками (экономика, движение флота, цвета/карта, бой с игроком/корсаром, обозначения). При каждой загрузке показывается, пока в `localStorage` не выставлено **`gs.entryTutorial.suppress.v1`** галочкой «Больше не показывать при входе» + «Ок»; затемняющий фон не закрывает окно без «Ок». В подвале кнопка **«Напомнить»**. Bump **`GAME_VERSION`** → `01.068` (`me.html`, `me.js`, `styles.css`).
- Туториал: добавлены **протяжка флота** (drag-and-drop между клетками) и пояснение **стрелок на периметре** как смещения окна карты (+ двойной клик при объектах на границе). Bump **`GAME_VERSION`** → `01.069` (`me.html`).
- Туториал `/me`: **вкладки слева** по темам, цветные акценты вкладок, чипы/полоска легенды и текстовые **цветные выделения**; ввод упоминает вызов **«Напомнить подсказку»** внизу страницы; локаль **`me.js`** — `setEntryTutorialTab`. Bump **`GAME_VERSION`** → `01.070` (`me.html`, `me.js`, `styles.css`).
- Туториал: карточка **~80vw × 80vh** (с ограничениями по `100vw`/`100dvh` и `max-width: 1800px`), адаптивные отступы оверлея, шире колонка вкладок, `clamp` для заголовков и текста. Bump **`GAME_VERSION`** → `01.071` (`styles.css`).
- Туториал: общий **`.overlay-card { width: min(520px…) }`** перебивал ширину гида — селектор **`.overlay-card.entry-tutorial-card`**. Bump **`GAME_VERSION`** → `01.072`.

- Форпост **`/me`**: модалка объекта своего форпоста — **потребление за тик** (база + дельта модулей, итог), вклад модулей в обзор/бой, **улучшение корпуса** (`structure_upgrade`), компактный список установленных модулей (**разобрать** / **улучшить** с проверкой `prereq_tech` апгрейда), установка **корневых** модулей при свободных слотах (из **`outpost_modules`** в **`GET /api/balance`**). API: **`POST /api/outposts/modules/dismantle`** (`dismantle_outpost_module` — возврат **`slot_cost_engineers`** на инженерный флот на клетке). **`get_sector_stub`**: **`_outpost_stats(..., viewer_player_id=…)`** для своих — флаги **`can_upgrade_module`**, недостающие tech и upkeep. Выбор клетки: модалка открывается и для **своего форпоста** без планеты; успех **стройки постройки** не закрывает модалку на пустых клетках. Bump **`GAME_VERSION`** → **`01.073`** (`world_service/_mixin_01`, `_mixin_03`, **`routes_progress.py`**, **`routes_buildings_fleets_combat.py`**, **`me.js`**).

- **Имперское снабжение флотов**: расход с капитала не «1 металл + 1 кристалл за флот любого размера», а сумма по **каждому кораблю** — блок **`units.*.upkeep.empire_per_sol`** (`metal`, `crystal`, `food`, `water`); опционально глобальные **`economy.fleet_empire_upkeep.*_per_sol_per_fleet`** и **`.*_per_sol_per_ship`**. Расчёт в **`BalanceService.calc_units_empire_supply_upkeep`**; учёт через **`_fleet_empire_supply_need_for_fleet`**, **`_apply_fleet_empire_upkeep_tick`** списывает все четыре ресурса разом («всё или штраф энергии»). Превью империи в **`economy_service`**, UI «Империя» — еда/вода в строке содержания флотов. Тики пополнения/аварии флота идут по **`_fleet_total_units`**, не по устаревшему **`Fleet.qty`**. Bump **`GAME_VERSION`** → **`01.074`** (`units.json`, `economy.json`, **`balance_service.py`**, **`_mixin_02.py`**, **`economy_service.py`**, **`me.js`**, **`test_balance`**).

- **`_outpost_stats` / 500 на `/me` и окне карты:** в блоке `supply_line` локальная переменная **`hp`** при загрузке планеты-хаба затеняла боевые **HP** форпоста → `TypeError: int() ... not 'Planet'` в `combat`. Переименовано в **`hub_row`**. Сопутствующие команды к БД: **`python -m alembic current`** → **`upgrade head`** → **`current`** (head **`20260503_000019`**). Bump **`GAME_VERSION`** → **`01.076`** (`world_service/_mixin_01.py`).

- **Планетарные постройки (баланс + UI):** «Полевой пайок» → **`Ферма`**, апгрейд сразу в **`hydro_farm`** («**Гидропоника**»); «Простой опреснитель» без хвостов в названии → апгрейд в **`atmospheric_reclaim`** («**Атмосферный конденсатор**»). Для сохранений со **`starter_farm` / `starter_water`** добавлены апгрейды в те же цели. **`upgrade_building`:** без повторного `engineer_required` для построек **`planet_id`**, учёт лимита **`max_per_planet`** по **`_resolve_building_id`** (смешанные ключи в БД). Карточка модалки: **🔧 / ♻** вместо длинных подписей; уровень **(ур.)** только если **> 1**. Модалки **планета / империя / флот** — **`place-items: start`** + **`padding-top`**, ниже липкого топбара. Империя: заголовок **«Империя — экономика»**, блок **«три шага»** и переименованная секция **«Производство и расходы»**. Bump **`GAME_VERSION`** → **`01.077`**.

- **Модалки `/me` (экономика, планета, форпост, флот):** модалка **Империя** переведена на **`game-modal-grid`**: слева **чистый приток** и дом, по центру производство / расход / запасы+горизонт, справа **«Главный расход»** (статья с максимальной суммой по затрачиваемым ресурсам). Пояснения — в **`details`**; галочка внешних построек в шапке. **Колония:** центр — блоки «Состояние» / «Снабжение» / «Склад» (очередь стройки строкой под складом); постройки на поверхности — **`details`** «Постройки на поверхности». **Форпост:** «Содержание (за сол)», пояснение про дом в **`title`**, кнопка **«Улучшить форпост…»**, модули и установка в **`details`** с явными summary. **Флот:** исправлен лишний **`</b>`** в строке автономности; стили **`fleet-ship-acc`**, **`fleet-ship-slot`**, превью апкипа. Bump **`GAME_VERSION`** → **`01.078`** (`me.js`, `styles.css`).

- **Апгрейд планетных построек:** убрана проверка **`max_per_planet`** целевого типа в **`upgrade_building`** (`planet_type_cap`). Новая постройка по-прежнему ограничена **`build_slots_total`**; лимит «4 гидропоники» при «6 фермах» ломал улучшение фермы → гидропоника. Bump **`GAME_VERSION`** → **`01.080`** (`world_service/_mixin_04.py`).
- **Системный лог боя:** после опроса состояния заново выставлялся `innerHTML`, и **`<details>`** «Подробный расчёт» схлопывался. На **`#events`** — делегированный **`toggle`**, множество **`openFleetCombatDetailIds`** по **`data-event-id`** (числовой **`events.id`** из API); после **`renderEvents`** раскрытые блоки восстанавливаются. Bump **`GAME_VERSION`** → **`01.081`** (`me.js`).
- **Системный лог — читаемость боя:** шапка **«Сол N — Победа (457:104)»** + строка **«Alpha» vs «Beta»** (`matchup_line`, `scores_compact` в **`player_story`**, рендер в **`me.js`**); блок **Подробности боя**; потери **`−N кораблей (14%)`** + разбиение типом второй строкой; порядок: потери → **флот противника уничтожен** → клетка; доли потерь «расчёт» только внутри подробностей. Событие **`combat_prompt_arrival`**: **`enemy_fleet_name`**, акцент **`.event-combat-prompt`**, строка ⚠ «Бой: враг …». Bump **`GAME_VERSION`** → **`01.082`** (`_mixin_05.py`, **`_mixin_06.py`**, **`me.js`**, **`styles.css`**).

- **Баг лога потерь флота:** при **`fleets.qty` больше суммы `fleet_ships`** виртуальный «дабб» из `_fleet_units_map` считался **до боя**, а после **`_write_fleet_units`** в ленте только строки БД → **«потеряли» призраков** (типично лишний десяток). Перед боем **`_materialize_legacy_fleet_qty_into_ship_rows`** (`_mixin_01.py`), вызывается из **`_resolve_fleet_vs_fleet_combat`**. **`_composition_casualties`**: `lost_total` согласован с суммами состава при рассихроне ключей. UI: процент **`(N%)`** у потерь считается от **before/after** реального состава (`me.js`). Bump **`GAME_VERSION`** → **`01.084`**.

- **Профилактика «призрачного qty»:** при каждом **`get_world_state`** для всех флотов оператора вызывается **`_materialize_legacy_fleet_qty_into_ship_rows`** (`_mixin_06.py`); в **`_fleet_units_map`** добавлен предупреждающий комментарий. Тест **`test_world_state_materializes_inflated_fleet_qty`** (`test_auth_and_world.py`). Bump **`GAME_VERSION`** → **`01.085`**.

- **Модалки планета / флот / исследования (`/me`):** постройки на поверхности — группировка **Еда · Вода · Прочее** с заголовком вида **«Название ×N»** и сеткой иконок по экземплярам. Флот: **содержание за сол** и короткая справка в **левой** колонке; **склад до сохранения** убран; корабли в **центре** только с **`details`** по строке корабля. Исследования: карточка **`modal-shell-wide`**, двухколоночная сетка; слева **запасы науки**, **модификаторы**, **поиск**, **вертикальный фильтр**; центр — список без **id техов** на виду и без строки «не начато»; предпосылки/поля/полный текст — **`?`** через **`abbr title`**; галочку «скрыть завершённые» убрали (`me.html`, **`me.js`**, **`styles.css`**). Bump **`GAME_VERSION`** → **`01.083`**.

- **`POST /api/buildings/upgrade` 500:** после снятия `planet_type_cap` в **`upgrade_building`** блок с **`planet = …`** удалили, а сброс населения к **`_effective_max_population`** оставили → **`NameError: planet`**. Восстановлено **`planet = s.get(Planet, row.planet_id) if row.planet_id else None`** после **`flush`**. UI: кнопка **🔧 улучшить** неактивна, если в кэше баланса нет валидной цели **`upgrade.to`** (как на сервере). Bump **`GAME_VERSION`** → **`01.086`** (`_mixin_04.py`, **`me.js`**).

- **Постройки на планете (группа ×N):** вместо сетки дублей ♻🔧 одна строка из **трёх** действий: снести **одну** (первая в списке клетки), улучшить **одну**, при **≥2** и есть апгрейд в балансе — **🔧∞** «улучшить все» по очереди с **`skipUiRefresh`** между шагами и одним финальным обновлением. Bump **`GAME_VERSION`** → **`01.087`** (`me.js`, **`styles.css`**).

- **Статусы и блок действий построек:** сообщения после стройки/апгрейда — **название из баланса** (`buildingStatusCaptionRu`), напр. *«Улучшено до „Атмосферный конденсатор“»*, не сырое `atmospheric_reclaim`. Ошибки **`tech_required`** (стройка построек/апгрейд/форпосты/модули) показывают **имена техов** из кэша баланса. В **`details`** «Постройки на поверхности» добавлен русский текст с пояснением ♻ / 🔧 / 🔧∞; подсказки кнопок скорректированы. Bump **`GAME_VERSION`** → **`01.088`** (`me.js`).

- **Приватный чат — прочитано, счётчик, удаление темы:** у **`chat_messages`** поле **`read_receipt_at`** (уведомление отправителя, если собеседник включил режим во «входном» модальном диалоге); таблица **`private_chat_peer_prefs`** (**`welcomed_at`**, **`send_read_receipts`**, **`last_read_incoming_id`**, локальное **`hidden_at`** «удалить чат из списка»). **`GET /api/chat/private/threads`** — счётчики **`badge_new_contacts` / `badge_unread`**, у темы **`last_message_at`**, **`unread_incoming`**; **`GET /api/chat/private/badge`**, **`GET/POST`** meta/open, **`PATCH /thread/prefs`**, **`POST /thread/hide`**. Коммит после **`GET /api/chat/private`** при успехе (фиксируются прочтения). UI `/me`: бейдж **«Приват»** **`новые/непрочитанное`**, модалка при первом открытии, чек «Сообщать о прочтении», «Удалить чат», время последнего сообщения в списке. Миграция **`20260510_000021_private_chat_reads.py`**. Bump **`GAME_VERSION`** → **`01.090`**.
- Если миграция не применена (**`UndefinedColumn chat_messages.read_receipt_at`** / «ошибка сети» в привате), при включённом **`GUARDSTAR_DB_SAFETY_NET`** (дефолт) **`apply_dev_schema_safety_net`** дозаполняет **`read_receipt_at`** и создаёт **`private_chat_peer_prefs`** идемпотентно (`server/app/db/dev_schema_safety_net.py`). На продакшене — **`alembic upgrade head`** до **`20260510_000021`** (или актуальной головы).

- Документация для внешних клиентов (боты, интеграции): **`docs/api-player.md`** — префикс **`/api`**, сессия по cookie, **`GET /health` / `/ready` / `/version`**, таблицы игровых эндпоинтов, типовые JSON и мини-сценарий «логин → баланс → окно → постройка». Расширение: раздел «Расширенный учебник» — **curl**, **`building_aliases`**, примеры **`/me`** и **`/world/state`**, ошибки **`place` / `tech/start`**, форпосты/чат, цикл агента.

- **Исследования — панель задач слева:** блок **«Задачи исследований»** под запасами науки — **текущее** исследование с прогресс‑баром и **аббривиатуры** (сол старта/конца, остаток), **«Ранее»** — последнее по `finish_sol` среди завершённых (тултип). Вкладка **«В работе»** убрана. При занятой очереди кнопка **«Запустить»** отключена с подсказкой про левую панель; ответ **`tech_queue_full`** — понятное сообщение вместо сырого кода ошибки. Предпросмотр «Скоро доступно» оценивает готовность зависимостей без учёта очереди (карточки остаются, запуск второго всё равно недоступен). **`GAME_VERSION` → `01.095`** (`me.js`, `styles.css`).

- **Колонка «Скоро доступно» — маршрут:** кликабельная цепочка зависимостей (**✔/✖** и стрелки), строка **`N/N условий · до открытия`** (плюрализация «исследований»), кнопка **«Построить путь»** (фильтр центра только по недостающим шагам, подсветка `tech-item-path-focus`, баннер **«Снять маршрут»**, авто‑скролл к выбранной плашке), клик по плашке — то же что путь к цели и прокрутка к технологии. Заголовок колонки: **«Скоро доступно»**. Сброс режима при закрытии модалки. **`GAME_VERSION` → `01.094`** (`me.js`, `styles.css`).

- **Модалка исследований — третья колонка «Вскоре»:** технологии, у которых **не выполнены прямые prereq**, при этом **у самой технологии полевые данные уже есть**, и **каждая недостающая зависимость** удовлетворяет тем же правилам, что и кнопка «Запустить» в основном списке (цепочка prereq + field-data). Дальние ветви (типа «Корона»), где зависимости сами недоступны, сюда не попадают. Дубликаты исключены из средней колонки. Фон‑водяной знак колонки, прокрутка третьей колонки сохраняется при рефреше. Bump **`GAME_VERSION`** → **`01.093`** (`me.js`, `styles.css`).

- **Топбар ресурсов (`/me`):** в **`GET /api/world/state` → `economy.net_per_sol`** возвращён тот же смысл, что у **`GET /api/economy/summary`** (чистый приток/расход по империи за сол по складам ресурсов). В HUD чип ресурса при **`net_per_sol.[ресурс] < 0`** получает класс **`res-chip--drain`** (красноватый фон рамки, фильтр иконки, маркер **`!`**). RP в топбаре без режима расхода. **`GAME_VERSION` → `01.096`** (`_mixin_06.py`, `me.html`, `me.js`, `styles.css`, **`docs/api-player.md`**, **`test_auth_and_world.py`**).
- **`/me` — модалка исследований:** при авто‑обновлении (`loadTechModalContent` после `/api/world/state`) сохраняется и восстанавливается **прокрутка** (`#tech-modal-body`, `.tech-modal-main`, `#tech-modal-list`). Черновик **поиска** в `localStorage` (`gs.techModal.v1.search`). Поле поиска в сайдбаре **не растягивается по высоте** (`.tech-modal-sidebar .tech-search`). Подсказка теха тянет **`/api/economy/summary`**: производство ресурсов в формате «сейчас +N/сол → после +M/сол», оценка **энергии на флоты**, пояснения к снабжению и бою. **`economy.json`:** усилены **RP**/лаборатория (`home_capital_per_sol` **0.13**, `research_lab_t1_per_sol` **0.17**), немного усилены **strain** лабораторий под новые цены исследований. Bump **`GAME_VERSION`** → **`01.092`** (`me.js`, `styles.css`).

- **`cell_occupied_by_own_fleet` при одном живом флоте:** в БД могла оставаться «оболочка» **`fleets`** с фактически **0** кораблей по `_fleet_units_map` (игрок её не видит в `/api/world/state`, а проверки занятости клетки смотрели только на наличие строки). **`_purge_shell_fleets_at_cell`** в `_mixin_01` удаляет такие записи через **`_write_fleet_units(..., {})`** (с **`fleet_disbanded`** в логе). Вызывается перед приказом **`POST /api/fleets/move`**, перед проверками прилёта тика (**`_mixin_06`**), **`_nearest_cell_without_other_fleet`**, **`_enemy_fleet_at`**, **`_cell_blocked_for_fleet`**; **`estimate_fleet_combat_preview`** использует **`_enemy_fleet_at`**. Интеграционный **`test_fleet_move_prunes_empty_shell_at_target_cell`**. Bump **`GAME_VERSION`** → **`01.097`** (`_mixin_01.py`, **`_mixin_05.py`**, **`_mixin_06.py`**, **`test_auth_and_world.py`**).

- **Рекалибровка дерева технологий (`data/balance/tech.json`):** 46 узлов, **`tier` 1–10** с текстовыми эпохами **I / II / III** (рубежи у **5** и **10**). Стартовый слой быстрый и дешёвый по RP/тикам и ресурсу; с **tier 5+** резко выше `research_points_cost`, `residual_time_ticks` и `cost`. Перекрёстные `prereq` между логистикой, промышленностью, боем и снабжением; сохранены **все прежние `id`**, на которые завязаны постройки/юниты (**`tech_supply_networks_1`**, **`tech_deep_scan_1`**, территории, гидропоника/вода, энергосеть, промышленность II и т.д.). Добавлены ветви тиров 5–10 до трёх **апексов** и **`tech_crown_sector_hegemony`**. Bump **`GAME_VERSION`** → **`01.091`**.

- **Позывные операторов и публичный профиль:** таблица **`reserved_display_names`** (уникальный **`name_norm`** = пробелы-схлоп + **`casefold`**, снимок отображения, **`player_id` ON DELETE SET NULL** — имена остаются заблокированными). Регистрация и переименование — валидация **≥3** символа, ошибки **`DisplayNameInvalid`** (`empty` / **`too_short`** / **`too_long`** / **`taken`**); **`POST /api/register`** те же коды. Публичная страница **`GET /operator/<uuid>`** (`operator_public.html`) — только позывной, история других резервирований этим аккаунтом, UUID для поля ЛС + копирование; без хеша ключа. **`/account`** — полный кабинет (ключ, переименование и т.д.), ссылка на публичный профиль. Топ игры: позывной — ссылка на профиль; чат общий / приват / объекты на клетке / влияние — ссылки **`/operator/…`**; плейсхолдер ЛС указывает на профиль. Миграция **`20260504_000020_reserved_display_names.py`**. Bump **`GAME_VERSION`** → **`01.089`**.
