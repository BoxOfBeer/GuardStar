# Dev-log 2026-05-03

Продолжение линии UI/MMO и админки от **2026-05-02**; новые фиксы и версии — **с этой даты** в этом файле.

- Разборка монолитного `server/app/__init__.py`: safety-net SQL → `app/db/dev_schema_safety_net.py`, импорт моделей → `app/db/models_registry.py` (в т.ч. `ChatMessage`, `PlayerBlock` для `metadata`), пост-старт → `app/startup.py`. Поведение `create_app()` без изменений смысла.
- Монолит `world_service.py` → пакет `app/services/world_service/`: `_deps.py`, `constants.py`, шесть миксинов `_mixin_01`…`_mixin_06`, публичный `WorldService` в `__init__.py`; импорты `from app.services.world_service import WorldService` без смены. Тесты `test_balance` читают исходники пакета целиком.
- Монолиты `routes/api.py` и `routes/web.py` → пакеты `app/routes/api/` и `app/routes/web/` (blueprint + `common` + сегменты маршрутов); `from app.routes.api import api_bp` / `web_bp` без смены.
- По API: `routes_progress_chat.py` → `routes_progress.py` + `routes_chat.py`; убран `deps.py` и `from deps import *` во всех сегментах — **явные импорты** в каждом `routes_*.py` (раньше при загрузке тянулся весь слой; теперь видно зависимости и меньше риск теневых имён).
- Web: `public_routes` / `admin_routes` — явные импорты вместо `deps.py` (файл удалён). `world_service`: во всех `_mixin_*.py` убран `from constants import *`, константы импортируются явным списком; `_deps import *` пока оставлен (ORM/сервисы — отдельный большой шаг).
